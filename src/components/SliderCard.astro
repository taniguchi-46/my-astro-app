---
import Card from "./Card.astro";

interface Props {
  id?: string;
  title: string;
  subtitle?: string;
}

const props = Astro.props;
---

<Card {...props} class="h-full">
  <slot />
  {Astro.slots.has('footer') && (
    <slot name="footer" slot="footer" />
  )}
</Card>
