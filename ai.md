# AI 指示書 — KAIRO'S LAB

このファイルはリポジトリを開いた AI が最初に読む引き継ぎ文書。
現在の状態は `status.md` を参照すること。

---

## プロジェクト概要

- **サイト名**: KAIRO'S LAB
- **目的**: 自宅サーバー公開 + ポートフォリオ兼ブログ
- **オーナー**: Kairo
- **公開先**: 自宅サーバー（Linux Mint + Docker + Nginx）経由で外部公開
- **開発環境**: Windows ローカルで開発し、GitHub 経由でサーバーに反映

---

## 技術スタック（確定）

| 項目 | 内容 |
|---|---|
| フレームワーク | Astro v6 |
| CSS | Tailwind CSS v4（`@tailwindcss/vite` 経由） |
| コンテナ | Docker / Docker Compose |
| リバースプロキシ | Nginx |
| Node.js | v22.12.0 以上 |
| 言語 | TypeScript / Astro コンポーネント |

---

## デザインルール（変更禁止）

**設計思想**: ソリッド・ミニマリズム。バンド「フレデリック」のような太い枠線によるグリッド分割。装飾を削ぎ落とした工業的な美学。

**カラーパレット**:

| 用途 | 値 |
|---|---|
| ベース背景（1次） | `#EFEAE2`（強いベージュ） |
| コンテンツ背景（2次） | `#FFFFFF`（白） |
| 主要テキスト | `#202836`（深いインディゴスレート） |
| サブ文字 / 紹介文 | `#4A5568` |
| 薄文字 / 注記 | `#A0AEC0` |
| アクセント（赤） | `#D60000`（鮮烈なインダストリアル赤） |
| サブアクセント | `#990000` |

**フォント**:
- 本文: `Noto Sans JP`（日本語対応）
- コード / 番号 / 英字見出し: `Fira Code`

**枠線**: 主要な境界線は `3px` の `#202836`。カード内部は `1.5px`。

---

## ディレクトリ構成

```
src/
├── assets/          静的アセット（画像など）
├── components/      再利用コンポーネント
│   ├── Article.astro    ブログ記事カード
│   ├── Footer.astro     フッター（「臥薪嘗胆」配置）
│   ├── Header.astro     ヘッダー + ハンバーガーメニュー（3本線）
│   ├── Loader.astro     ローディング画面
│   └── Welcome.astro    （未使用・テンプレート残骸）
├── content/         Markdown コンテンツ（ブログ記事）
├── layouts/
│   └── Layout.astro     全ページ共通レイアウト
├── pages/           ファイルベースルーティング
│   ├── index.astro      BLOG（トップページ）
│   ├── profile.astro    PROFILE
│   └── links.astro      LINKS
└── styles/
    └── global.css       Tailwind エントリポイント（`@import "tailwindcss";`）
```

---

## ページ構成（確定）

| URL | ページ名 | 役割 |
|---|---|---|
| `/` | BLOG | ブログ記事一覧（トップページ） |
| `/profile` | PROFILE | 自己紹介 + インフラ構成 + リンク |
| `/links` | LINKS | 外部リンクランチャー（カテゴリ別） |

---

## 作業ルール

1. **実装は宣言されてから動く。** ユーザーが「実装する」と言うまで、コードを変更しない。
2. **事実ベースで書く。** 推測・感想は書かない。確認できた状態だけ記録する。
3. **設計変更は必ず確認する。** カラー・フォント・枠線ルールを無断で変えない。
4. **1ステップずつ進める。** ユーザーが「次へ」と言うまで次のステップに移らない。
5. **status.md を更新する。** 作業完了時・保留発生時は必ず `status.md` を更新する。

---

## status.md の更新ルール

- **更新タイミング**: セッション開始時（現状確認）・作業完了時・保留発生時
- **書き方**: 箇条書き・事実のみ・過去形で完了を記録
- **削除しない**: `## 完了したこと` は累積記録。古いものも残す。
